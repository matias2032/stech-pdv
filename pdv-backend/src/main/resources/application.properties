
# E:\pdv-stech engenharia\pdv-backend\src\main\resources\application.properties(para testes locais)

# spring.datasource.url=jdbc:postgresql://localhost:5433/stech_pdv

# # Usuário do PostgreSQL
# spring.datasource.username=postgres

# # Senha do PostgreSQL
# spring.datasource.password=hx1232


spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# Driver JDBC do PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# ===================================
# CONFIGURAÇÕES DO JPA/HIBERNATE
# ===================================
# Dialeto do PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=none

# Mostra os comandos SQL no console (útil para debug)
spring.jpa.show-sql=true

# Formata o SQL no console (mais legível)
spring.jpa.properties.hibernate.format_sql=true


# application.properties
spring.docker.compose.enabled=false

# Corrige problema com tipos LOB no PostgreSQL
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Desativa warning de metadata obsoleta
spring.jpa.open-in-view=false

# ===================================
# CONFIGURAÇÕES DO SERVIDOR
# ===================================
# Porta do servidor (padrão 8080)
server.port=8080

# Contexto da aplicação (URL base)
server.servlet.context-path=/

# ===================================
# CONFIGURAÇÕES DE ENCODING
# ===================================
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

spring.jackson.time-zone=Africa/Maputo
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss

# ===================================
# CONFIGURAÇÕES DE LOG
# ===================================
# Nível de log geral
logging.level.root=INFO

# Log da aplicação (debug para desenvolvimento)
logging.level.com.loja1.projectohibrido=DEBUG
# Log SQL do Hibernate
logging.level.org.hibernate.SQL=DEBUG

# Log dos parâmetros SQL (mostra valores dos placeholders ?)
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


logging.level.org.springframework.security=DEBUG




spring.datasource.hikari.maximum-pool-size=10


spring.datasource.hikari.connection-timeout=20000


spring.datasource.hikari.max-lifetime=1800000

# Tempo de inatividade antes de fechar conexão (ms)
spring.datasource.hikari.idle-timeout=60000000

spring.datasource.hikari.initialization-fail-timeout=-1

# ===================================
# CONFIGURAÇÕES DE VALIDAÇÃO
# ===================================
# Ativa validação de beans
spring.jpa.properties.jakarta.persistence.validation.mode=auto




firebase.service-account.path=firebase-service-account.json

# Activa o suporte a métodos @Async no Spring
# (adicionar @EnableAsync na classe principal da aplicação)
spring.task.execution.pool.core-size=4


sync.cloud.base-url=https://stech-pdv.onrender.com
sync.cloud.api-key=local-key-mock123
sync.api-key=local-endpoint-key-321


spring.flyway.enabled=false